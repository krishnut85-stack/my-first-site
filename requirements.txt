pandas>=2.0
numpy>=1.24
PyYAML>=6.0
python-dateutil>=2.8
kiteconnect>=5.0   # quotes + orders via Zerodha
requests>=2.31     # Kite TOTP login + Telegram alerts
pyotp>=2.9         # TOTP for unattended daily Kite login
pytest>=8.0        # for running the test suite
